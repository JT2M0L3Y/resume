name: Serve Resume

on:
  push:
    branches:
      - main

jobs:
  release:
    uses: ./.github/workflows/release.yml
  
  build:
    needs: release
    uses: ./.github/workflows/build.yml
  
  deploy:
    needs: build
    uses: ./.github/workflows/deploy.yml
